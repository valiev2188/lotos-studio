# Deployment is handled automatically:
# - Vercel: connected to GitHub, auto-deploys on push to main
# - Railway: connected to GitHub, auto-deploys on push to main
# No CI workflow needed — remove to avoid blocking Railway deployments
