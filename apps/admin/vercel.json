{
  "buildCommand": "cd ../.. && pnpm --filter @lotos/admin build",
  "outputDirectory": "apps/admin/dist",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
